<view class="classic-container" hidden="{{hidden}}">
    <image src="{{img}}" class="classic-big"></image>
     <image src="./images/movie@tag.png" class="classic-small"></image>
     <text class="classic-content">{{content}}</text>
</view>